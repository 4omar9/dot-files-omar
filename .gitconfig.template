[user]
    name = YOUR NAME
    email = your.email@example.com

# Shared aliases, LFS, etc. Put anything *generic* here.
[alias]
    co = checkout

# Example: rewrite Bitbucket HTTPS to SSH (remove if not needed)
# [url "git@bitbucket.org:"]
#     insteadOf = https://bitbucket.org/
